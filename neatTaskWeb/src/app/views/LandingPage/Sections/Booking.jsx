import React from "react";

// styled components
import { BookingForm } from "../../../containers";

const Booking = (props) => {

  return (
    <div>
      <BookingForm />
    </div>
  );
};

export default Booking;
