import React from "react";

// styled components
import { BookingForm } from "../../../containers";

const Product = (props) => {

  return (
    <div>
      <BookingForm />
    </div>
  );
};

export default Product;
